# Necessário para o build (Vercel/local): @capacitor-firebase/authentication declara
# peerOptional firebase ^10||^11, mas o projeto usa firebase 12 (compatível no fluxo
# que usamos). Sem isto, o `npm install` da Vercel falha com ERESOLVE e o deploy quebra.
legacy-peer-deps=true
